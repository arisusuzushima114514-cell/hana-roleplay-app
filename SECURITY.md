# Security

If you discover a security issue, please do not publish secrets, tokens, or private endpoints in public issues.

When reporting security-sensitive problems, describe:

- affected area
- impact
- reproduction conditions
- whether secrets may be exposed

Before publishing the repository, re-check:

- API keys are not committed
- local configuration files are excluded
- release artifacts are excluded
- debug-only settings are not exposed as production defaults
